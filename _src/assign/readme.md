```js
import { assign, merge } from "rimjs/assign"
// or am.assign am.merge
import am from "rimjs/assign"

// assign 同 Object.assign，不同之处是 这个为深度克隆

// merge 是将连个对象合并（深度）
// merge 对于数组，不会重置，只会合并
```
